### Extract all books from http://www.gutenberg.org/ to a python list

1. download and extract http://www.gutenberg.org/cache/epub/feeds/rdf-files.tar.zip. You may need to repeat the extracting process multiple times until you end up with a folder called `cache`.
2. Rename `cache` to `rdf_info`.
3. Run `gutenburg_reader.py`
